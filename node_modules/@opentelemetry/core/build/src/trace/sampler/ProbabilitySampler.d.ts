import { Sampler, SpanContext, SamplingDecision } from '@opentelemetry/api';
/** Sampler that samples a given fraction of traces. */
export declare class ProbabilitySampler implements Sampler {
    private readonly _probability;
    constructor(_probability?: number);
    shouldSample(parentContext?: SpanContext): {
        decision: SamplingDecision;
    };
    toString(): string;
    private _normalize;
}
export declare const ALWAYS_SAMPLER: ProbabilitySampler;
export declare const NEVER_SAMPLER: ProbabilitySampler;
//# sourceMappingURL=ProbabilitySampler.d.ts.map